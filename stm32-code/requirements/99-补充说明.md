## 第 1 次补充需求

* PA1（水泵继电器）：存在电路设计缺陷，但代码完整保留

- PA2（风扇继电器）：存在电路设计缺陷，但代码完整保留
- 按键分配（K1模式切换/K2水泵/K3风扇/K4消警报）——合适
- 串口调试日志是阶段9联调时保留，在创新阶段就，即接入ESP8266阶段再将该口转交给ESP8266使用
- 植物类型阈值切换需要在这一阶段实现，
- 接下来探讨：
  1. 规划按键实施逻辑；
  2. 按键自定义设置阈值加减，并且同步OLED显示

## 第 2 次补充需求

* Row 0: 温度: 26.5°C   [H/L图标]
  Row 1: 湿度: 2130
  Row 2: 自动:P:OFF F:OFF
  Row 3: 状态:正常
* 需要补充给OLED的字模需要给出可行的解决方法
* 阈值存储：设置完成后需要将阈值写入 AT24C02，掉电保存
* 长按检测：按判定时间用 2 秒
* 湿度值方向：干燥时 ADC 值小
* 设置模式下：

1. Row 0: === 设置  ===
2. Row 1: > 植物: 常规/喜湿/耐旱
3. Row 2:     阈值: 1800
4. Row 3:     K1:下一个 K4:保存

* 需要确认显示是否达到OLED显示长度限制

## 第 3 次补充需求

* 设置模式 Row 3 改为 K1:下项 K4:保存   │ 确认
* ° 符号暂不显示
* 字模生成方式 ：我用 PCtoLCD2002 复制粘贴进led_font.h 中的 OLED_HZ16X16 数组，采用阴极、宋体设置，其他相同

## 第 4 次补充需求

* 烧录代码后按照清单测试发现，K4按键关闭报警之后无法再次开启报警，重新上电则可以正常报警 		`需修复`
* 长按K1进入设置模式，在设置中，OLED的ROW2： 湿度下：1700；
* 再按K1进入下项（3/6），OLED的ROW2： 湿度^：2200；
* 再按K1进入下项（4/6），OLED的ROW2： 温度^：20；
* 再按K1进入下项（5/6），OLED的ROW2： 温度！：30 C；
* 再按K1进入下项（4/6），OLED的ROW2： 湿！：1600；
* 即：存在中文显示的失败以及逻辑的错误，需修复为：
* 湿度下限、湿度上限、温度下限、温度上限
* 算上设置页也只有五页，而不是六页

## 第 5 次补充需求

* 湿度百分比转换函数中，应该是数值越大越湿润，映射关系需要更正，ADC 4095→100%，ADC 0→0%
* 设置按键步进改为 ±1%
* K4按键报警功能更改为为可手动启停，无需静音超时检测机制
